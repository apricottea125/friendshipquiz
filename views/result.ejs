<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz Result</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Crimson+Text&family=Protest+Revolution&family=Silkscreen:wght@400;700&display=swap" rel="stylesheet">
</head>
<body>
    <h1>Quiz Result</h1>
    <p>Thank you for submitting your answers!</p>
    <div>
        <h2>Your Score: <%= score %> / <%= totalQuestions %></h2><br>
        <h3>Percentage Correct: <%= Math.round(score / totalQuestions * 100) %>%</h3> <!-- Calculate percentage of correct answers -->
    </div>

    <div>
        <p>Here are the correct answers:</p><br>
    </div>
    <ol>
      <% correctAnswers.forEach((answer, index) => { %>
        <li>
          <%= index + 1 %>. <%= questions[index] %> <!-- Display question -->
          <br><span style="margin-left: 20px;"><%= correctAnswers[index] %></span> <!-- Display correct answer -->
        </li><br>
      <% }) %>
    </ol>

    <div>
      <% if (score / totalQuestions >= 0.75) { %>
          <h3>Wow! You know your friend so well!</h3>
      <% } else if (score / totalQuestions >= 0.5) { %>
          <h3>You know your friend quite well!</h3>
      <% } else { %>
          <h3>Looks like you need to get to know your friend better!</h3>
      <% } %>
  </div>
</body>
</html>

